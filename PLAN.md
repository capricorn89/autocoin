# 크립토 테스트베드 개선 프로젝트

## 배경

이 폴더는 예전에 코인 알고리즘 트레이딩용으로 만든 프로젝트야.
지금부터 이걸 **KRX 주식 알고리즘 매매 시스템의 선행 검증용 테스트베드**로 개선할 거야.

새로 만드는 게 아니라 **기존 코드를 살려서 개선**하는 게 목적이야.
코인 시장은 24시간 돌아가고 데이터 접근이 쉬우니까, 여기서 시그널이랑 백테스트
로직을 먼저 검증한 다음 같은 구조를 KRX 오더북 데이터로 이식할 계획이야.

---

## Phase 0 — 코드베이스 감사 (이거 끝나기 전에 코드 건드리지 마)

먼저 현재 프로젝트를 읽고 아래 항목을 정리한 감사 보고서를 뽑아줘.

1. **디렉토리 구조**: 주요 폴더/파일이랑 각각의 역할
2. **데이터 계층**: 어떤 거래소 API 쓰는지, 수집 방식(REST/WebSocket),
   저장소(파일/DB), 스키마
3. **시그널 계층**: 구현된 시그널/피처가 있는지, 있으면 계산 방식이랑 입력 데이터
4. **백테스트 계층**: 백테스트 엔진 있는지, 체결 가정(슬리피지/수수료/지연) 처리 방식
5. **실행 계층**: 주문 실행 로직, 페이퍼트레이딩 모드 유무, 리스크 제한 유무
6. **설정/시크릿 관리**: API 키를 어떻게 다루는지 — 하드코딩돼 있는지 꼭 확인해
7. **테스트/로깅**: 테스트 코드랑 로깅 체계 현황
8. **기술 부채**: 재사용할 부분 / 리팩터링 필요한 부분 / 버릴 부분을
   각각 근거 달아서 분류해줘

보고서 마지막에 **아래 목표 구조랑 현재 상태의 갭 분석**을 표로 정리해줘.
Phase 0 보고서 뽑고 나서는 멈추고, 내가 확인할 때까지 기다려.

---

## 목표 구조 (Phase 1 이후 작업 계획)

### M1. 거래소 API 연동
- 거래소 선정 근거 정리 (수수료 / API 안정성 / 오더북 depth / rate limit)
- API 인증 모듈을 별도 파일로 분리 — 인증 로직이 비즈니스 로직에 섞이지 않게
- WebSocket 오더북 스트림 수집 스크립트 (재연결 / heartbeat / 시퀀스 갭 처리 포함)

### M2. 데이터 파이프라인
- TimescaleDB 기반 저장 (Docker 구성 포함, 백업 정책부터 먼저 정의해)
- 틱/오더북 저장 스키마 설계 — hypertable 파티셔닝 기준 명시할 것
- 데이터 정합성 검증 스크립트 (결측 구간, 시퀀스 갭, 타임스탬프 역전 탐지)

### M3. 시그널 / 백테스트
- 시그널 3종 구현: TxnImbalance, PastReturn, LobImbalance
- 세 시그널 모두 동일한 인터페이스 따를 것 (입력: 오더북 스냅샷/체결 스트림,
  출력: 표준화된 시그널 값)
- **predict-before-backtest 원칙**: 시그널의 예측력(IC, 수익률 분위수 등)을
  먼저 검증하고, 그 결과가 유의미할 때만 백테스트로 넘어가.
  백테스트 성과가 좋다는 이유로 예측력 검증을 건너뛰지 마.
- 백테스트 엔진에 코인 데이터 소스 연결
- 시그널별 성능 비교 리포트 자동 생성

### M4. 페이퍼트레이딩 / 소액 검증
- 페이퍼트레이딩 모드 (실주문 없이 시그널→가상체결 경로 전체를 태우는 모드)
- 소액 실전 주문 실행 모듈
- 리스크 한도: 포지션 사이즈 상한, 일일 손실 한도, 강제 청산 조건
- 1주 소액 검증 후 결과 리뷰 리포트

---

## Obsidian 로깅 연동 (M2에 포함, 우선순위 높게)

작업 로그랑 실험 기록을 Obsidian 볼트에 자동으로 남기게 만들어줘.

**저장 위치**: 기존 볼트 경로(환경변수 `OBSIDIAN_AUTOJI_PATH`) 하위의
`crypto_testbed/` 폴더. 이 폴더는 이미 만들어놨어.

환경변수가 없거나 `crypto_testbed/` 폴더가 존재하지 않으면 **경고가 아니라
즉시 실패(hard fail)** 시켜. 조용히 넘어가면 로그 유실된 걸 나중에 알게 돼.

**폴더 구조** (하위 폴더는 없으면 자동 생성):
```
$OBSIDIAN_AUTOJI_PATH/crypto_testbed/
├── 00-index.md              # 전체 진행 상황 대시보드
├── 01-decisions/            # 설계 결정 기록 (거래소 선정, 스키마 설계 등)
├── 02-experiments/          # 시그널 검증 / 백테스트 실행 기록
├── 03-daily-logs/           # YYYY-MM-DD.md 형식 일일 작업 로그
└── 04-incidents/            # 데이터 유실, API 장애 등 사고 기록
```

**기록 규칙**:
- 실험 노트 파일명: `YYYY-MM-DD_<실험명>.md`
- 실험 노트 frontmatter에 실행 시각, 데이터 구간, 파라미터, 커밋 해시,
  결과 요약 수치를 남겨
- 결정 기록에는 **채택한 안뿐 아니라 기각한 대안이랑 기각 사유**도 같이 남겨
- 나중에 Linear 이슈랑 상호 참조할 수 있게, 노트 제목에 이슈 ID를 넣을 수 있는
  형태로 써줘 (예: `[CRY-12] 거래소 선정 검토`)
- 노트 안에서 다른 노트를 참조할 땐 Obsidian 위키링크(`[[...]]`) 문법 써
- `00-index.md`는 마일스톤별 진행 상황이랑 최근 실험 노트 링크를 담고,
  각 마일스톤 끝날 때마다 갱신해

---

## 작업 원칙

- **기존 코드 우선**: 재작성보다 개선을 택해. 전면 재작성이 필요하다고 판단되면
  먼저 근거 제시하고 내 확인 받아.
- **단계별 확인**: 각 마일스톤 끝날 때마다 멈추고 결과 보고해. 여러 마일스톤을
  한 번에 진행하지 마.
- **시크릿 금지**: API 키/시크릿을 코드나 커밋에 절대 넣지 마. `.env`랑
  `.gitignore`로 관리하고 `.env.example`만 커밋해.
- **실주문 안전장치**: 실주문 낼 수 있는 코드 경로엔 반드시 명시적 플래그가
  필요하게 만들어. 기본값은 항상 페이퍼트레이딩이야.
- **가정 명시**: 코드에 반영한 가정(수수료율, 슬리피지 모델, 체결 지연 등)은
  주석이랑 Obsidian 결정 노트 양쪽에 남겨.

---

일단 Phase 0 감사부터 시작해줘.
