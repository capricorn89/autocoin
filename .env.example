# 이 저장소에는 시크릿을 두지 않는다.
#
# 1) API 키: 저장소 밖 ~/.config/autocoin/.env 에 입력 (권한 600)
#      BINANCE_API_KEY=...
#      BINANCE_API_SECRET=...
#    다른 위치를 쓰려면 AUTOCOIN_ENV_FILE 환경변수로 지정. 저장소 안 경로는 코드가 로드를 거부한다.
#
# 2) 비밀이 아닌 환경변수 (셸 프로필 ~/.zshenv 등에 export)
OBSIDIAN_AUTOJI_PATH=
